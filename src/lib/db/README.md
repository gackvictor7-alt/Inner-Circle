# Database module (planned – STEP 04)

Relational database: **PostgreSQL** (managed free tier, e.g. Neon) with
**Prisma ORM** + versioned migrations.

Schema is introduced phase by phase (see `docs/02-architecture.md`).
STEP 01 intentionally ships without a database dependency so the
foundation runs anywhere.
